package com.bank.transfer.service;

import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
class AccountTransferServiceImplTest {
//    private static final Integer ACCOUNT_TRANSFER_ID=1;
//    @Mock
//    private AccountTransferRepository accountTransferRepository;
//    @InjectMocks
//    private AccountTransferServiceImpl accountTransferService;
//    @Test
//    public void allAccountTransfer() {
//        given(accountTransferRepository.findAll()).willReturn(Optional.of());
//        accountTransferService.
//
//        Mockito.doReturn(Optional.of(new AccountTransfer(ACCOUNT_TRANSFER_ID)))
//                .when(accountTransferRepository).findById(ACCOUNT_TRANSFER_ID);
//    }
//    @Test
//    void getHistoryById() {
//        when(repository.findById(HISTORY_ID))
//                .thenReturn(Optional.of(HISTORY));
//        when(mapper.historyToDto(HISTORY))
//                .thenReturn(HISTORY_DTO);
//
//        HistoryDto actualResult = service.getHistoryById(HISTORY_ID);
//
//        assertEquals(HISTORY_DTO, actualResult);
//        verify(repository).findById(HISTORY_ID);
//        verify(mapper).historyToDto(HISTORY);
//    }
}